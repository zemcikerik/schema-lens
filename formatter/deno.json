{
  "tasks": {
    "check": "deno check --frozen --all main.ts",
    "dev": "deno serve --cached-only --check=all --port=8000 --watch --allow-net main.ts",
    "run": "deno serve --cached-only --allow-net --port=8000 main.ts"
  },
  "imports": {
    "@std/expect": "jsr:@std/expect@1",
    "@std/http": "jsr:@std/http@1",
    "sql-formatter": "npm:sql-formatter@^15.4.10"
  }
}
